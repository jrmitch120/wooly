# AGENTS.md

## Agent skills

### Issue tracker

Issues and specs are tracked as GitHub Issues in `jrmitch120/wooly`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the five canonical triage roles as-is (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout — `CONTEXT.md` at the repo root. See `docs/agents/domain.md`.
